'use strict';

appControllers.controller('LoginController', LoginController);

LoginController.$inject = ['$scope', 'datafactory', '$sce'];

function LoginController($scope, datafactory, $sce) {
	var vm = this;
}